{
  "name": "protobuf-lite",
  "version": "0.1.0",
  "description": "protocol buffers, much simplified",
  "keywords": [ "pack", "unpack", "protocol", "buffer", "protobuf", "protocol buffers" ],
  "main": "protobuf.js",
  "author": "Andras",
  "repository": "https://github.com/andrasq/node-protobuf-lite#readme",
  "license": "Apache-2.0",

  "dependencies": {
    "ieee-float": "0.6.0",
    "q-utf8": "0.1.1"
  },
  "devDependencies": {
    "qnit": "0.19.0",
    "qtimeit": "0.21.0"
  },
  "scripts": {
    "test": "qnit test-*",
    "benchmark": "node benchmark.js",
    "coverage": "nyc --reporter lcov --reporter text npm test",
    "clean": "rm -rf .nyc_output coverage npm-debug.log"
  }
}
